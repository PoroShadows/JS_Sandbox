function Model(element) {
    if (typeof element === 'object' && element.hasOwnProperty('tag')) {
        for (var prop of Object.getOwnPropertyNames(element)) {

        }
    }
}

Model({
    getTest: function () {
        "use strict";
    }
});